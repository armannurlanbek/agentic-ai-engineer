# LLM & Orchestration
openai>=1.40.0
langgraph>=0.4.0
langchain-openai>=0.3.0
langchain-core>=0.3.0

# Web Framework
fastapi>=0.115.0
uvicorn[standard]>=0.32.0
python-multipart>=0.0.18

# RAG / Vector Store
# Pinned to the exact version that wrote data/chroma_db/ — the persistence format
# is version-sensitive, so a newer chromadb on the server can fail to read it.
chromadb==1.5.9

# Search & Discovery
tavily-python>=0.5.0

# Web Scraping
httpx>=0.28.0
beautifulsoup4>=4.12.0
feedparser>=6.0.10
lxml>=5.0.0

# Document Processing
pypdf>=5.0.0
python-docx>=1.1.0
tiktoken>=0.8.0

# Data Validation & Config
pydantic>=2.10.0
pydantic-settings>=2.7.0
python-dotenv>=1.0.0
